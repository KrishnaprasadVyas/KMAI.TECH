import type { Testimonial } from '../types';

// Testimonial architecture is prepared for genuine client feedback.
// Only display testimonials marked as `approved: true`.
// Do NOT invent fake quotes or placeholders.
export const testimonials: Testimonial[] = [];
